import { createTheme } from '@mui/material/styles';

const theme = createTheme({
    palette: {
        primary: {
            main: '#b2e9dd',
            light: '#b0dedf',
            dark: '#07a3a2',
            contrastText: '#000',
        }
    },
});

export default theme;